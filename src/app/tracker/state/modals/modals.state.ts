export interface ModalsState {
  currentHP: boolean;
  maxHP: boolean;
  tempHP: boolean;
  tempMaxHP: boolean;
  name: boolean;
  concentration: boolean;
  unconscious: boolean;
  characterMenu: boolean;
}
